#include <bits/stdc++.h>
using namespace std;
int main(){
      char str[20]="abc";
      char str2[20]="ytr";
    //  strcpy(str,str2);//it copy value of one to other
    //  strcat(str,str2); //it joint the two diff arr string
    cout<<strcmp(str,str2);// if 1st one string is greater in value the value is +1,if equal then 0, if 2nd is greater than -1 
     cout<<str;
      return 0;
}